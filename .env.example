# Brain (ChatGPT). Demo dies without this unless a rehearsal fallback hits.
OPENAI_API_KEY=

# Voice (Grok TTS). If missing, steps still show; browser speech is the last ditch.
XAI_API_KEY=

# Optional receipts push. Local file is always the source of truth.
ALCHEMYST_AI_API_KEY=
